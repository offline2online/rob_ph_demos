# Help Centre rewrite — TODO(verify) items (109)

Items that no source confirmed; each is removed from the public HTML and phrased so the surrounding text stands without it. Verify against the platform and edit the article in the console (or `faq/data/articles/<id>.json`).

| Article | Item |
|---|---|
| Connect the platform to Google Maps / Places (`faq-art-51000290754`) | TODO(verify): the Find Address control and search box inside the Add New Store(s) dialog were not captured in ui-reference.md; label taken from the 2022 source article. -- |
| Obtain your Google Business Profile API credentials (`faq-art-51000290791`) | TODO(verify): the callback path /location-api/auth/google/callback comes from the 2022 source article; confirm it is unchanged in the current release, and confirm https is required. -- |
| Connect the platform to Google Business Profile (`faq-art-51000290792`) | TODO(verify): field labels inside the Connect Platform with Google form (Client ID, Client Secret, Secret/Token drop-down) were not captured in ui-reference.md because the demo tenant is Not Connected; labels taken from the 2022 source article. -- |
| Connect the platform to Google Business Profile (`faq-art-51000290792`) | TODO(verify): exact label — the 2022 source calls it 'Authenticating with Google Business Profile'. -- |
| Connect the platform to Google Business Profile (`faq-art-51000290792`) | TODO(verify): the Manage Synchronised Stores flow and its labels come from the 2022 source article and could not be exercised in the demo tenant. -- |
| Back up your Google Business Profile data (`faq-art-51000290793`) | TODO(verify): Google Business Profile Manager labels (Actions, Download, Locations) are from the 2022 source article; confirm against the current Google UI. -- |
| ChromeOS: register a Chromebox with Google CDM and assign your kiosk app (`faq-art-51000343747`) | TODO(verify): the Chrome Web Store app ID of the Personalisation Hub kiosk player for ChromeOS is not stated in the sources; confirm the ID (or the app name to search for) and where customers obtain it. -- |
| ChromeOS: set up a new organisational unit in Google CDM (`faq-art-51000325086`) | TODO(verify): Google Admin console navigation and setting-group names are from the 2023 source article; confirm against the current console. -- |
| Install the AEM Screens player on Mac or PC (`faq-art-51000325427`) | TODO(verify): the source article (Freshdesk 51000325427) has no body ('tbd'). Write the full procedure: where to download the AEM Screens player for Mac and Windows, the install steps, how to point the player at your AEM instance and register it, and how to verify playback. -- |
| Install the AEM Screens player on Mac or PC (`faq-art-51000325427`) | TODO(verify): the only related Scribe ('Delete a Location / Store from AEM') covers unpublishing and deleting a location in AEM, not installing the player — decide whether it needs its own article. -- |
| Enable appointment bookings (`faq-art-51000483636`) | TODO(verify): whether the Appointment Settings tab has its own Save button or the Enable Appointments toggle applies immediately; ui-reference.md lists a "Save changes" button only on the Queue Settings tab -- |
| Set appointment slots per store and per day (`faq-art-appointment-slots`) | TODO(verify): confirm that the per-day "# of Appointment Slots" on Store Hours overrides the template's "Default # of Appointment Slots" for that day, and which value applies when the per-day field is left at its default -- |
| Block out dates and times for appointments (`faq-art-calendar-blocking`) | TODO(verify): the Add New Calendar Block form was not opened during capture; field labels are taken from the table columns (Block-Out Period Name, Date, Time, Recurrence). Confirm the form's labels, whether a block can be limited to specific stores, and the name of its save button -- |
| Appointment SMS and email notifications (`faq-art-appointment-messages`) | TODO(verify): the wording of each appointment template, and the tokens available in them (for example ${VisitorFirstName}, ${BrandName}, ${StoreName}), were not captured; only the queueing SMS texts are documented in corpus/51000360033.md. Also confirm whether templates are editable in Shared Settings (the table shows a Last Updated column) or only by Personalisation Hub support, as the original queueing article states -- |
| Upload your brand guidelines for the AI agents (`faq-art-brand-guidelines`) | TODO(verify): what the BRAND GUIDELINES USED BY AI-AGENTS/SKILLS panel shows after an upload (file name, a Remove/Replace action), whether a Save Changes is needed, and the maximum file size. |
| Upload your brand guidelines for the AI agents (`faq-art-brand-guidelines`) | TODO(verify): whether a scanned, image-only PDF is read by the agents; the troubleshooting entry assumes text extraction. Also confirm that campaigns already created are unaffected by a replaced guidelines file. |
| Define target audiences and campaign objectives (`faq-art-campaign-settings`) | TODO(verify): whether the new row is confirmed with Enter, a tick icon or a Save button — the add control was not captured. -- |
| Define target audiences and campaign objectives (`faq-art-campaign-settings`) | TODO(verify): what happens to a campaign whose selected audience or objective is deleted. -- |
| Upload Retail Admin users for your stores (`faq-art-51000483613`) | TODO(verify): exact column headings of the CSV template (name, email, role, store code). -- |
| Integrate single sign-on (SAML) for store staff (`faq-art-51000359801`) | TODO(verify): whether CSV-created accounts continue to work once SAML is enabled, or are replaced. -- |
| Sign in to the Retail Admin site (no SAML/SSO) (`faq-art-51000358534`) | TODO(verify): exact field labels on the Retail Admin login and change-password forms — not captured in the UI reference. -- |
| Add custom menu links (CTA settings) to HQ Admin and Retail Admin (`faq-art-cta-settings`) | TODO(verify): the add-category dialog and its confirm control were not captured; field names are taken from the table columns. -- |
| Add custom menu links (CTA settings) to HQ Admin and Retail Admin (`faq-art-cta-settings`) | TODO(verify): whether CTA links open embedded inside the admin frame or in a new tab; if embedded, the destination must allow framing (see faq-art-51000359560). -- |
| How campaigns work: playback strategies, touch-points and status (`faq-art-campaign-concepts`) | TODO(verify): the platform assigns the Playback Strategy pill automatically; confirm the exact rule used to classify a campaign as SCHEDULED vs TARGETED vs LOCALISED when it has both schedules and targeting rules. |
| How campaigns work: playback strategies, touch-points and status (`faq-art-campaign-concepts`) | TODO(verify): exact definition of each status value, in particular the difference between Draft and Not Activated, and what moves an Activated campaign to Not Running. |
| Launch a new campaign (`faq-art-launch-a-campaign`) | Resolved 16 September 2026 from screenshots of the dialog: the field is "Talk to me about your campaign?" (placeholder "Describe your campaign in as much detail as possible"), the submit button is "Add New Campaign", and the dialog lists 6 captured details (Campaign Name, Campaign Detail, Promoted Product, Campaign Objective, Touch Points, Target Audience). TODO(verify): when Add New Campaign becomes enabled (it was disabled both with an empty field and mid-dictation), and what the tick and spinner states on the captured-details list mean. |
| Launch a new campaign (`faq-art-launch-a-campaign`) | Resolved 16 September 2026: the manual route is the banner "If you'd prefer to Manually Add the Campaign click here" at the top of the dialog. TODO(verify): what the click here link opens (an empty Campaign Brief tab or a separate manual form) and the label of its save control; the article says "the form that opens". |
| Launch a new campaign (`faq-art-launch-a-campaign`) | Resolved 16 September 2026: the microphone icon sits inside the description field at its bottom right; while dictating it changes to a red pause control and the spoken text appears in the field. |
| Generate storyboard, copy and creative with AI (`faq-art-generate-creative`) | TODO(verify): exact label of this button; the walkthrough video says "generate storyboard and copy" |
| Generate storyboard, copy and creative with AI (`faq-art-generate-creative`) | TODO(verify): exact label of this button; the walkthrough video says "generate campaign preview" |
| Generate storyboard, copy and creative with AI (`faq-art-generate-creative`) | TODO(verify): ui-reference.md notes the Storyboard & Copy tab is disabled when the campaign is Not Activated; confirm the exact condition that enables it |
| Add a campaign to play on a digital signage display (`faq-art-51000491547`) | TODO(verify): accepted file formats and maximum file size for campaign assets |
| Add a campaign to play on a digital signage display (`faq-art-51000491547`) | TODO(verify): accepted file formats and maximum file size for campaign assets -- |
| Schedule a campaign (`faq-art-51000500898`) | TODO(verify): exact field labels and Mode option names in the Add New Schedule form; the form itself was not captured in ui-reference.md |
| Target a campaign (`faq-art-51000500901`) | TODO(verify): the complete list of options in the first (data source) select |
| Target a campaign (`faq-art-51000500901`) | TODO(verify): the complete list of options in the first (data source) select -- |
| Activate or deactivate a campaign (`faq-art-activate-campaign`) | TODO(verify): exact label and position of the bulk action; it comes from the Scribe walkthrough and did not appear in the captured list view with nothing selected |
| Prioritise which campaigns play first (`faq-art-campaign-prioritisation`) | TODO(verify): whether Campaign Prioritisation also changes the playback order on digital signage displays. The Scribe walkthrough only confirms the mobile-site campaign carousel, and selecting the button in the demo tenant produced no visible dialog. |
| Prioritise which campaigns play first (`faq-art-campaign-prioritisation`) | TODO(verify): how the per-playlist Priority number on the Creative tab relates to Campaign Prioritisation |
| Localise campaigns with store segments (`faq-art-51000360858`) | TODO(verify): how a new segment row is confirmed (Enter key or a save control) |
| Localise campaigns with store segments (`faq-art-51000360858`) | TODO(verify): how a new segment row is confirmed (Enter key or a save control) -- |
| Target campaigns at specific displays with display tags (`faq-art-51000360912`) | TODO(verify): how a new tag row is confirmed (Enter key or a save control) |
| Target campaigns at specific displays with display tags (`faq-art-51000360912`) | TODO(verify): how a new tag row is confirmed (Enter key or a save control) -- |
| Localise campaigns by visitors' reason for visit (`faq-art-51000508014`) | TODO(verify): whether the reason-for-visit category and the percentage are set in the same row, and the exact field labels |
| Target passers-by using computer vision at the edge (`faq-art-51000508033`) | TODO(verify): the live article title mentions age as well as gender, but the documented steps only cover Gender; confirm whether an age variable exists under Visitor / Customer Data |
| Enable digital signage (`faq-art-51000491524`) | TODO(verify): exact field labels on the ChromeOS / Android sub-tab; it did not render in the UI capture. Values are inferred from the enable-digital-signage video. |
| Register a new display or device (`faq-art-51000491526`) | TODO(verify): the button label. Today's UI helper text says "Register Display"; the older articles say "Register Device" and "Register". |
| Create a display type and playlist (`faq-art-51000506169`) | TODO(verify): exact field labels on the Add Display Type form (name, width, height, description, touch point); the form was not opened in the UI capture. |
| View a display's status and time offline (`faq-art-51000360571`) | TODO(verify): the exact list of offline-period options in the Status filter; the Scribe shows only "30 Days". |
| Get notified when a display goes offline (`faq-art-51000290858`) | TODO(verify): exact field labels and option lists on the Create New Notification form; only the list columns and one example row (1 Day, Daily) were captured. |
| View a display's log history (`faq-art-51000360577`) | TODO(verify): the 2023 Scribe calls this tab "Device Log"; today's UI labels it "Device Status" at route /display-issues-log. |
| Filter displays by the channel they are playing (`faq-art-51000360735`) | TODO(verify): whether the Active Playlist column has its own filter; the UI capture lists filters only on Status, Type, Name, Store Code, Store Name, Playback Audit, State and Enabled Features. |
| Find all the identifiers for a display (`faq-art-51000360796`) | TODO(verify): the 2023 Scribe also lists an "AEM Display Path" field for AEM-based deployments; it is not present on the Display Details tab in today's UI capture. |
| Enable chatbot interactions on a display or mobile site (`faq-art-51000360815`) | TODO(verify): the 2023 Scribe shows this checkbox on the "Active Integrations" tab. Today's Enabled Features tab (same route) shows "Allow AI-Agents to Control Campaign Playback on Display" and no chatbot checkbox; confirm which control now enables chatbot interactions. |
| Use cases supported by Personalisation Hub (`faq-art-51000495815`) | TODO(verify): the magic-mirror / AR experience is named in the use-cases overview but no set-up article or UI setting for it was found in the sources. -- |
| Use cases supported by Personalisation Hub (`faq-art-51000495815`) | TODO(verify): push notification on arrival is described in the use-cases overview; the sources do not show where in the platform it is configured (it is likely sent by the retailer's own app). -- |
| Where is my data stored and how is the platform secured? (`faq-art-security-and-data`) | TODO(verify): confirm exactly which campaign data is sent to external AI model providers and whether any customer or visitor data is ever included; the sources show the provider table and base URLs only. -- |
| Update a campaign's interaction settings (`faq-art-51000359234`) | TODO(verify): Platform Admin → API Authentication shows a platform-wide "Clear Visitor Data from Display after 2 minutes" setting. Confirm whether it caps or overrides the per-campaign value. |
| Enable QR Control and set the default QR code settings (`faq-art-51000359275`) | TODO(verify): the location of the platform-wide default QR code settings for landscape and portrait displays was not captured in ui-reference.md. Manage Display Types has a "Display Settings" column and a View/Edit action; confirm the default QR fields live there and capture their labels. |
| Change the QR code size, colour or position on a display (`faq-art-51000359276`) | TODO(verify): the QR code position control for an individual display was not captured under Enabled Features in ui-reference.md (only Size, Qr Code Colour, Connected Icon/Logo Colour and template). Confirm the position field's label and options. |
| Let retail staff activate triggered campaigns (`faq-art-51000359277`) | TODO(verify): confirm whether the Retail Admin "Active Campaign" select on the display detail is the control staff use to trigger playback, and whether the campaign stops after the campaign's "Clear Campaign/Interaction Data after" period or when staff clear the field. |
| Connect a triggered campaign to an AI agent interaction (`faq-art-51000359319`) | TODO(verify): where the synonyms/utterances for a triggered campaign are entered was not captured in ui-reference.md (A.1.2 shows no synonyms field). Confirm the field and its tab. |
| Connect a triggered campaign to an AI agent interaction (`faq-art-51000359319`) | TODO(verify): the Scribe (2023) labels the checkbox "Enable Chatbot / Voice Recognition"; ui-reference.md (2026) shows "Allow AI-Agents to Control Campaign Playback on Display" under Enabled Features. Confirm the current label. |
| Use customer and decisioning data to personalise the store visit (`faq-art-51000361015`) | TODO(verify): the source only describes the two JSON payloads; the exact schema for device_type, events and page_view was not reproduced. Capture a verbatim sample payload from the live article. |
| Connect your mobile app or third-party system to the Visitor API (`faq-art-51000361042`) | TODO(verify): the token names for Store Code and Display ID in the "+ Add Token" list (for example ${StoreCode} and ${DisplayId}) were not captured in ui-reference.md; the source article's Scribe steps were not recoverable. Confirm the exact token names and whether tokens are added on the menu item URL or the Mobile Header field. |
| Connect AI models and providers (`faq-art-ai-models`) | Resolved 16 September 2026 from a screenshot of an existing integration's edit form (`/platform-admin/platform-integrations/ai-integrations/7`): fields are Integration Name, Provider (list), Model (list, e.g. LTX 2.5 Pro), Base URL (optional, placeholder "Enter API base URL") and API Key, with Cancel / Save Changes. TODO(verify): confirm the + Create Integration form shows the same fields as the edit form. |
| Connect AI models and providers (`faq-art-ai-models`) | TODO(verify): how a skill's AI Model / Integration assignment is changed on the AI Agents / Skills → System Prompts tab (inline select or edit dialog) and the name of its save control; the article says "assign one of yours" without naming the control. |
| AI agents and skills reference (`faq-art-ai-agents`) | TODO(verify): the Role and Responsibilities column values for each skill were not captured; the "Stage" and "Produces" columns are inferred from the skill names and the Campaign detail tabs. Capture the on-screen Role/Responsibilities text. |
| AI agents and skills reference (`faq-art-ai-agents`) | TODO(verify): the Vector DB tab rendered no distinct content during capture; document its fields. |
| Enable proximity personalisation with MIST (`faq-art-proximity-mist`) | TODO(verify): how a campaign is targeted at a proximity event (which condition type appears in the Targeting tab for a zone or vBeacon match) was not captured. Confirm and add a step. |
| Step 1 — Deploy the platform through AWS Marketplace (`faq-art-51000481977`) | TODO(verify): confirm the current AWS Marketplace button labels (View Purchase Options / Subscribe / Continue to Launch / Launch) against the live listing — AWS changes them periodically |
| Step 1 — Deploy the platform through AWS Marketplace (`faq-art-51000481977`) | TODO(verify): the exact labels of the three CIDR parameters on the stack details page are not in the sources |
| Step 4 — First login and platform set-up (`faq-art-51000463143`) | TODO(verify): exact label of the confirm/update-password button on the first-login page |
| Step 6 — Add your stores from Google Places (`faq-art-51000483547`) | TODO(verify): the field and button labels inside the Add New Store(s) dialog (search box, manual-add option, confirm button) were not captured in ui-reference.md |
| Step 7 — Restrict platform access by IP address (`faq-art-51000463209`) | TODO(verify): the list of paths that must remain public behind a CDN is not in the sources |
| Step 7 — Restrict platform access by IP address (`faq-art-51000463209`) | TODO(verify): the field labels and save button inside the Add New IP Address form were not captured; the table columns are Description, Approved IP Address(es) [CIDR Schema] and Activation Status |
| AWS deployment: resources provisioned (`faq-art-51000496164`) | TODO(verify): the sources list the services and component types but not exact resource counts or names; confirm against a live stack before publishing figures |
| AWS deployment: testing and troubleshooting (`faq-art-51000496186`) | TODO(verify): corpus/51000496186.md names this output EC2InstancePublicIP; the installation video and Step 2 use EC2InstancePublicURL — confirm the output key on a live stack |
| AWS deployment: lifecycle management (`faq-art-51000496197`) | TODO(verify): the sources do not describe how to cancel the subscription or delete the stack (order of operations, data retention) |
| AWS deployment: lifecycle management (`faq-art-51000496197`) | TODO(verify): the on-screen label in corpus/51000496197.md is "Just Functional"; shortened here because house style bans the word — confirm how the label should be shown |
| Add reason-for-visit categories and sub-categories (`faq-art-51000359322`) | TODO(verify): the sample spreadsheet attachment (telco and banking examples) was not captured in the corpus; confirm it is still available and where it is hosted. |
| Add reason-for-visit categories and sub-categories (`faq-art-51000359322`) | TODO(verify): the save control for a new reason-for-visit row was not captured in ui-reference (no save was clicked during capture); confirm its label. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): the exact rule behind the "Dynamic" option is not stated in the sources; the (info) tooltip was not captured. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): whether the manager's "Place on Hold" option also depends on this toggle is not stated in the sources. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): the behaviour behind "Sale Check-In" is inferred from the label only; the (info) tooltip was not captured. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): the behaviour behind "Capture Final Outcome" is inferred from the label only; the (info) tooltip was not captured. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): the precise behaviour of the "skip their place" window is not described in the sources. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): the link between this timer and the no-show SMS is inferred; confirm. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): which report uses this value and the currency were not captured. |
| Queue settings reference (`faq-art-queue-settings-reference`) | TODO(verify): which report uses this value and the currency were not captured. -- |
| Set up a dedicated concierge display or kiosk (`faq-art-51000363265`) | TODO(verify): the pre-configured menu item's exact label; the 2023 Scribe shows "Join the queue" and the demo tenant shows a similar item named "Queue for Table". |
| Update your profile photo (`faq-art-51000359875`) | TODO(verify): whether the picture uploads immediately on selection or only after Save Changes was not captured. |
| Add a visitor to the queue manually (`faq-art-51000358424`) | TODO(verify): the reason-for-visit selector was not visible on the manual form in the demo tenant capture; confirm where it appears in the manual add flow. |
| Pick up a visitor from the queue (`faq-art-51000359918`) | TODO(verify): the label of the control that ends a visit (for example "Finish" or "Complete") was not captured; the queue was empty in the demo tenant and the Scribe stops at "Start". |
| Place a visitor on hold (`faq-art-51000359919`) | TODO(verify): the exact location and label of the staff-level on-hold control (visitor details page or In Progress card) was not captured; the queue was empty in the demo tenant. |
| Add additional information to a visitor in the queue (`faq-art-51000360014`) | TODO(verify): the exact label of the "Add Additional Info" control on a queue card was not captured; the queue was empty in the demo tenant and the source Scribe was not retrieved. |
| Retail Admin shows 'Lost Connection to Queueing Server!' (`faq-art-queue-lost-connection`) | TODO(verify): causes beyond a dropped network connection (queueing server restart, platform maintenance) are not documented in the sources. |
| Add a store (`faq-art-add-a-store`) | TODO(verify): exact labels of the search field, the selection control and the manual-add option inside the Add New Store(s) dialog were not captured in ui-reference.md -- |
| Update a store code (`faq-art-51000360360`) | TODO(verify): the AEM step-by-step for updating a location's store code was an embedded Scribe in the original article and is not in the sources; only the order (AEM first, then Personalisation Hub) is confirmed -- |
| Update a store code (`faq-art-51000360360`) | TODO(verify): whether existing Retail Admin users linked to the old code follow the store automatically or need to be re-uploaded with the new code -- |
| Add or update a store's address (`faq-art-51000360495`) | TODO(verify): whether the Address field offers Google Places suggestions as you type, or is free text only -- |
| View a store's network and display status (`faq-art-51000360500`) | TODO(verify): the filter options offered on the connected-displays column were not captured -- |
| Delete a store (`faq-art-51000360461`) | TODO(verify): what the platform does with displays, staff users and queue/appointment history that belong to a deleted store is not stated in the sources -- |
| Set special holiday opening hours (Retail Admin) (`faq-art-special-holiday-hours`) | TODO(verify): confirm that an unticked public holiday is treated as a closed day (as on the weekly Opening Hours tab) rather than falling back to normal weekly hours -- |
| Why create a mobile site for your stores? (`faq-art-51000359321`) | TODO(verify): exact trigger for Connected Website State; the label is on the template page but its behaviour is not described in any source -- |
| Define your default mobile site template (`faq-art-51000359492`) | TODO(verify): exact field labels inside the menu item editor (name, icon, URL, schedule) — only the row columns and the "Open in New Tab" field are captured in the sources -- |
| Add Google Maps directions to your mobile site (`faq-art-51000359588`) | TODO(verify): whether a store address token exists for menu item and campaign URLs (only ${StoreName} and ${BrandName} are confirmed in the sources) and whether the menu item URL field offers "+ Add Token" -- |
| Choose which campaigns appear in the campaign carousel (`faq-art-51000359638`) | TODO(verify): confirm that selecting a template under the Store Sites touch-point is what adds the campaign to that template's carousel; the field is captured in ui-reference.md#A.1.2 but the corpus only describes activation -- |
| A display shows as offline (`faq-art-display-offline`) | TODO(verify): that displays reconnect automatically without a restart once the network returns. -- |
| Adding a store finds nothing in Google Places (`faq-art-places-search-empty`) | TODO(verify): that the Places API must be enabled on the key and that billing is required — inferred from Google's requirements, not stated in the platform sources. -- |
