{
  "name": "backlog-tracker-rules-tests",
  "private": true,
  "description": "Tests for backlog-tracker's access-control layer (Firestore security rules, the MCP server's logic, the MCP server over real HTTP driven by the official MCP client SDK), for scripts/ helpers that are easiest to verify against a real disposable git repo (the faq/data/index.json merge-conflict resolver, and the deployment train's lock-recompute logic \u2014 pure predicate, the real Cloud Function trigger under stubbed SDKs, and the real branch-archive/reset git logic), and for the FAQ editor's content round-trip in a real browser (opt-in, needs Chromium).",
  "scripts": {
    "test": "npm run test:mcp && npm run test:client && npm run test:train-lock && npm run test:faq-index && npm run test:rules && npm run test:preview-url",
    "test:mcp": "node mcp-server.test.js",
    "test:client": "node mcp-client.test.mjs",
    "test:train-lock": "node train-lock.test.js && node train-lock-trigger.test.js && node train-lock-branch-archive.test.js",
    "test:faq-index": "node faq-index.test.js",
    "test:rules": "firebase emulators:exec --only firestore --project demo-backlog-tracker 'node firestore-rules.test.js'",
    "test:editor": "node faq-editor-load.test.mjs",
    "test:preview-url": "node preview-url-page.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dompurify": "^3.2.6",
    "firebase": "^12.0.0",
    "firebase-tools": "^13.0.0",
    "playwright-core": "^1.55.0",
    "quill": "1.3.7"
  },
  "//": "The firebase client version here is independent of the 10.13.2 SDK the app loads from gstatic: this one only drives the emulator, and the rules under test are evaluated server-side. It tracks whatever @firebase/rules-unit-testing peers against."
}
